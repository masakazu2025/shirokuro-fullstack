# 積み残しタスク

## 日付・オンライン状態の設計方針（確定）

### 基本思想：バック優先

フロントはバックエンドの状態を復元する。日付の制御権はバックエンドが持つ。

### 日付の更新ルール

バックエンドが起動時に全端末をプローブして JSON に書き戻す。
フロントは `list()` で取得した値をそのまま表示するだけ。日付の制御はバックが持つ。

| タイミング | online | date |
|---|---|---|
| マウント時 `list()` | 表示 | 表示（バックが正しい値を保証） |
| マウント時 `getStatus()` | 更新 | 触らない |
| 30秒ポーリング | 更新 | 触らない |
| ユーザー手入力（monitoring=off のみ） | — | DatePicker で編集可 |

### monitoring フラグの意味

- `monitoring=on`：指定日付モード。日付はシステム（ワーカー）が管理。ユーザーは編集不可。
- `monitoring=off`：手動モード。ユーザーが DatePicker で自由に日付を設定できる。

### 将来検討：最新日付モード（未実装）

`monitoring=on` のサブ設定として「日付を最新に保つ」オプションを検討中。
ワーカーの処理継続・中断の考慮が必要なため、実装コストが高く今は対象外。
実装時はチェックボックス＋ワーカーへの反映で対応できる見込み。

---

## バックエンド

- [x] バックエンド起動時に全端末をプローブして `online` + `date` を JSON に書き戻す
  `create_app()` 内でワンショットのプローブを実行し repo に保存する。
  これにより `online` の初期表示問題と `date` の起動時更新を両方解決する。
  （エンドポイントは不要。内部処理のみ）

- [x] `GET /terminals/status` から `date` を除去し `online` のみ返すように変更
  フロントはポーリングで `online` のみ使う設計に変わったため。
  net time は起動時プローブと将来の定期更新（未実装）のみで使う。

- [x] [将来] `GET /terminals/dates` エンドポイントの追加
  手動更新ボタンや定期更新オプションを実装する際に追加する。今は不要。

- [x] ping を `subprocess` から `socket` に切り替える
  現状の subprocess+pipe 方式は環境依存で不安定。
  `socket.connect_ex()` によるポート確認（135番など）に変えることで安定・高速化できる。
  → **スキップ**：対象環境が Windows 同士の構成のため、`ping.exe` は常に利用可能で十分安定している。
  socket 方式はポート（135等）がファイアウォールで閉じられた場合に誤 offline 判定するリスクがあり、恩恵より懸念が上回るため見送り。

## フロントエンド

- [ ] API エラーハンドリングの追加
  PATCH・DELETE 失敗時に UI がサイレントに成功扱いになっている。
  トーストや inline エラー表示を追加する。

- [x] フィルタ状態の永続化（検討）
  画面切り替えでフィルタがリセットされる。端末数が多い場合に手間になるため、
  セッション内で保持するか検討する。

- [ ] `applyFilters` を `useCallback` に移す（minor）
  現状は毎レンダー関数が再生成される。規模が大きくなったら対応。
