# 積み残しタスク

## バックエンド

- [ ] `online` ステータスを `terminals.json` に保存する
  現状は起動直後に全端末がオフライン表示になり、getStatus 完了まで不正確。
  getStatus の結果を repo に書き戻すか、起動時に即プローブする設計に変える。

- [ ] ping を `subprocess` から `socket` に切り替える
  現状の subprocess+pipe 方式は環境依存で不安定。
  `socket.connect_ex()` によるポート確認（135番など）に変えることで安定・高速化できる。

## フロントエンド

- [ ] API エラーハンドリングの追加
  PATCH・DELETE 失敗時に UI がサイレントに成功扱いになっている。
  トーストや inline エラー表示を追加する。

- [ ] フィルタ状態の永続化（検討）
  画面切り替えでフィルタがリセットされる。端末数が多い場合に手間になるため、
  セッション内で保持するか検討する。

- [ ] `applyFilters` を `useCallback` に移す（minor）
  現状は毎レンダー関数が再生成される。規模が大きくなったら対応。
