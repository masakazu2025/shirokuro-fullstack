[project]
name = "shirokuro"
version = "0.1.0"
description = ""
authors = [
    {name = "masakazu2025",email = "miz.masakazu87@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "shirokuro", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
